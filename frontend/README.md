Frontend (React + Vite + Tailwind)
-----------------------------------

Requirements:
- Node.js 16+
- npm or yarn

Run locally:
1. cd frontend
2. npm install
3. npm run dev
The frontend runs at http://localhost:5173 by default (Vite).

Make sure the backend is running at http://localhost:5000 for API calls.
