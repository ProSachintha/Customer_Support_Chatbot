# Optional: separate chatbot logic module (not required because app.py contains logic)
# This file is provided for clarity and could be imported by app.py instead.
# For simplicity, app.py currently implements the logic inline.
